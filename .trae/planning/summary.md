# 项目规划总结

## 1. 项目概述

### 1.1 项目目标

**主要目标：**
构建一个开源、企业级、一站式大模型APIKey管理与调度中枢平台，实现多厂商大模型APIKey的统一纳管、统一出口、统一鉴权、统一监控和统一计费。

**次要目标：**
- 通过虚拟Key隔离真实密钥，降低泄露风险
- 提供智能调度能力，提升可用性、降低成本、保障稳定性
- 支持私有化部署，满足企业合规要求
- 提供完整的监控、告警和审计能力

**成功标准：**
- 支持主流大模型厂商接入（OpenAI、通义、文心、豆包、Claude、Gemini、DeepSeek等）
- 统一接口兼容OpenAI格式，实现零代码迁移
- 平台可用性 ≥ 99.9%
- 单机QPS ≥ 500，接口平均响应 < 200ms
- Docker一键部署成功

### 1.2 目标用户

**主要用户：**
- **超级管理员 / 企业管理员（SUPER_ADMIN）**：企业级资源管理、团队创建与配置、系统维护，需要全局可控、权限清晰、运行稳定
- **团队管理员（Team.owner）**：团队成员管理、团队内发 Key、团队运营，需要低门槛、低歧义、团队上下文完整

**次要用户：**
- **普通用户/开发者**：调用模型、查看用量，需要稳定调用、简单接入、文档清晰
- **运维角色**：部署维护、监控告警，需要标准化部署、可观测性

**用户场景：**
1. 企业多团队共用AI资源，防止超支、滥用
2. 多模型统一接入，业务无需适配不同厂商接口
3. 密钥集中管理，避免代码硬编码、Git泄露
4. 高可用调度：自动切可用渠道，保障业务不间断
5. 私有化部署：数据不出内网，满足合规要求

## 2. 功能模块

| 模块ID | 模块名称 | 描述 | 优先级 | 状态 |
|--------|----------|------|--------|------|
| M001 | 系统基础模块 | 用户登录认证、角色权限管理、系统设置 | P0 | ✅ 已完成 |
| M002 | 渠道管理 | 渠道接入、渠道状态管理、模型管理 | P0 | ✅ 已完成 |
| M003 | APIKey管理 | 真实Key管理、虚拟Key管理 | P0 | 🟡 进行中 |
| M004 | 调度与代理网关 | 统一接入、调度策略、代理逻辑 | P0 | ⚪ 未开始 |
| M005 | 额度与计费 | 额度类型管理、计费规则、充值与套餐 | P0 | ⚪ 未开始 |
| M006 | 监控与日志 | 调用日志、数据看板、告警 | P0 | 🟡 进行中 |
| M007 | 用户与团队 | 团队管理、项目管理、成员管理 | P1 | 🟡 进行中 |
| M008 | 开放平台 | OpenAPI、Webhook、文档生成 | P1 | ⚪ 未开始 |
| M009 | 部署与运维 | Docker部署、健康检查、备份恢复 | P0 | ⚪ 未开始 |
| M010 | Auto模式 | 智能模型选择、角色模型映射、自动匹配 | P0 | ⚪ 未开始 |

### 模块详情

#### M001: 系统基础模块 ✅ 已完成

**功能列表：**
- 登录认证：账号密码登录、验证码、登录日志、IP记录、异常锁定、密码重置
- 角色权限：预设角色（超级管理员/管理员/普通用户）、自定义角色、权限粒度控制（菜单、操作、数据范围）
- 系统设置：基础信息配置、安全策略、告警通道配置、日志清理策略

**已实现功能：**
- ✅ Spring Boot 3.x + Java 17 项目结构搭建
- ✅ User/Role/Permission 实体与Repository层
- ✅ JWT Token认证 + Spring Security集成
- ✅ 用户登录接口（POST /api/v1/auth/login）
- ✅ 当前用户接口（GET /api/v1/auth/me）
- ✅ 统一响应格式（Result<T>、PageResult<T>）
- ✅ 全局异常处理（GlobalExceptionHandler）
- ✅ AES-256加密工具（AesEncryptUtil）
- ✅ Swagger/OpenAPI 3文档自动生成
- ✅ Redis缓存配置
- ✅ 登录成功后返回 `roles`，前端已做本地缓存

**技术实现：**
- JWT使用HS256签名算法，Token有效期2小时
- Spring Security 6.x配置，支持Bearer Token认证
- BCrypt密码哈希存储
- 白名单配置：/api/v1/auth/**、/actuator/**、/swagger-ui/** 无需认证

**依赖关系：**
- 为所有其他模块提供身份认证和权限控制基础
- 为监控与日志模块提供登录日志数据来源

**集成点：**
- 与所有业务模块集成，提供统一的身份认证和权限校验

---

#### M002: 渠道管理 ✅ 已完成

**功能列表：**
- 渠道接入：支持主流模型厂商、渠道类型配置、连通性测试
- 渠道状态：启用/禁用/维护中状态管理、健康度评估、自动禁用
- 模型管理：模型绑定、模型别名、模型上下线、统一模型目录

**已实现功能：**
- ✅ Channel/Model 实体与Repository层（含一对多关联）
- ✅ 渠道管理CRUD接口（6个REST端点）
- ✅ AES加密存储API Key + 掩码显示（sk-***...***abc格式）
- ✅ JPA Specification动态查询（关键词+类型筛选）
- ✅ @Async异步连通性测试
- ✅ 分页查询支持

**技术实现：**
- Channel实体22个字段完整映射，Model实体17个字段
- Channel与Model一对多关联（@OneToMany + @ManyToOne）
- LAZY加载策略避免N+1查询问题
- JpaSpecificationExecutor支持动态查询
- API Key使用AES-GCM加密存储

**依赖关系：**
- 依赖系统基础模块提供权限控制
- 为调度与代理网关提供候选资源池
- 为APIKey管理提供渠道承载对象

**集成点：**
- 与真实Key管理共同形成外部模型资源接入能力
- 为监控与日志模块提供渠道维度统计对象

---

#### M003: APIKey管理 🟡 进行中

**功能列表：**
- 真实Key管理：加密存储（AES-256）、列表展示（掩码显示）、批量导入/禁用
- 虚拟Key管理：生成规则（sk-xxx格式）、绑定用户/项目/模型/额度/限速、权限控制

**已实现功能：**
- ✅ RealKey实体与Repository层（13字段完整映射）
- ✅ AES加密存储Key值 + 掩码字段
- ✅ 与Channel的多对一关联
- ✅ RealKey管理接口（CRUD、分页查询、状态管理）
- ✅ VirtualKey实体与Repository层
- ✅ VirtualKey管理接口
- ✅ 团队上下文约束已进入实际联调阶段

**进行中功能：**
- 🟡 APIKey管理页面（前端）
- 🟡 团队上下文发 Key 链路稳定性收口

**技术实现：**
- RealKey实体：keyValueEncrypted（AES加密）、keyMask（掩码显示）
- VirtualKey实体：支持绑定用户、项目、模型、额度、限速配置
- Key生成规则：sk-{随机字符串}格式
- 支持过期时间设置和额度绑定

**依赖关系：**
- 依赖系统基础模块提供权限控制
- 依赖渠道管理提供渠道承载对象
- 为调度与代理网关提供鉴权对象

**集成点：**
- 与额度与计费模块共同完成调用前控制与调用后扣减
- 与监控与日志模块共同实现调用可追踪

---

#### M004: 调度与代理网关 ⚪ 未开始

**功能列表：**
- 统一接入：兼容OpenAI接口格式、请求自动路由、统一出口地址
- 调度策略：轮询/加权轮询/最低延迟/最低成本、故障自动转移、限流控制
- 代理逻辑：请求接收→鉴权→校验→调度→转发→记录→返回

**依赖关系：**
- 依赖虚拟Key管理提供鉴权对象
- 依赖渠道管理和模型管理提供资源基础
- 依赖额度与计费提供前置约束判断

**集成点：**
- 连接虚拟Key管理、额度与计费、渠道管理、模型管理、监控与日志的核心执行链路

---

#### M005: 额度与计费 ⚪ 未开始

**功能列表：**
- 额度类型：Token额度、调用次数额度、金额额度
- 计费规则：按Token/按次计费、模型单价配置、实时扣费、额度耗尽拦截
- 充值与套餐：企业套餐、时长套餐、充值记录（P2）

**依赖关系：**
- 依赖系统基础模块提供权限控制
- 为虚拟Key管理提供可绑定的资源约束对象
- 为调度与代理网关提供调用前拦截依据

**集成点：**
- 与调度与代理网关共同完成调用前校验和调用后扣减
- 与监控与日志共同沉淀费用统计

---

#### M006: 监控与日志 🟡 进行中

**功能列表：**
- 调用日志：全字段记录、检索导出、日志脱敏
- 数据看板：总调用量、Token消耗、费用、异常率统计、多维度分析
- 告警：额度不足、渠道异常、高频调用、异常IP告警、多通道通知

**当前状态：**
- 🟡 已有 Analytics / RequestLog 等前端页面基础
- 🟡 Analytics 当前已按团队 / 项目维度请求真实接口，不再依赖旧的 `/api/v1/users` 方案
- ⚪ 后端监控与告警体系仍未完全成型

**依赖关系：**
- 依赖系统基础模块提供权限控制
- 依赖调度与代理网关提供调用结果输出
- 依赖额度与计费提供消耗数据

**集成点：**
- 承接调度与代理网关的调用结果输出
- 为告警模块提供异常判定数据来源

---

#### M007: 用户与团队 🟡 进行中

**功能列表：**
- 团队管理：团队创建、成员管理
- 项目管理：项目隔离、资源独立
- 子账号配额管理
- 操作审计日志

**当前状态：**
- 🟡 团队管理不是纯规划态，已进入实际实现与联调阶段
- 🟡 当前产品边界已明确：企业管理员 = `SUPER_ADMIN`，团队管理员 = `Team.owner`
- 🟡 团队管理页已按双视角方向推进：
  - 企业管理员：创建团队、配置团队资源、指定团队管理员
  - 团队管理员：成员管理、团队内发 Key、团队上下文操作
- 🟡 当前重点是继续收口团队页双视角、成员管理、团队上下文发 Key

**依赖关系：**
- 依赖系统基础模块提供权限控制
- 与虚拟Key管理共同实现项目与成员的授权绑定

**集成点：**
- 与团队内发 Key、额度、模型分组权限控制共同形成企业内部资源治理能力

---

## 3. 当前执行备注

### 3.1 当前已确认

- `enterprise_admin` 已可成功登录
- 企业管理员视角当前由 `SUPER_ADMIN` 驱动
- 前端登录链路已稳定使用 `/api/v1/auth/login` + `/api/v1/auth/me`
- 登录成功后前端已缓存 `roles`
- `403` 不再触发强制退登

### 3.2 当前风险

- 运行态数据库不会因为 `db/data.sql` 改动自动补账号
- 已有环境的数据库状态、代码状态、规划文档状态可能漂移
- 后续如果保留 `enterprise_admin` 这类账号，应补正式初始化 / 迁移策略，而不是继续依赖人工补库

### 3.3 当前开发重心

当前不再以“修登录问题”为主，而是继续推进：
1. 团队管理双视角体验收口
2. 企业管理员 / 团队管理员权限边界收口
3. 团队上下文发 Key 链路稳定化
4. 视需要补正式 bootstrap / migration 方案
5. 打通 `team_members` 与团队可见性 / 项目可见性 / Key 工作区权限，不再只认 `Team.owner`
6. 优先复用已新增的 `POST /api/v1/users` 创建真实测试成员，再复用 `POST /api/v1/teams/{teamId}/members` 加入团队，避免回退到手工 SQL
